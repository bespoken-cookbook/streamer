- name: Test
  service: app
  command: npm test

- name: DockerHub push
  type: push
  service: app
  tag: master
  image_name: xappmedia/bst-streamer
  image_tag: "{{.Branch}}.Test"
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted
